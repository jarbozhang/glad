{
  "$schema": "../../node_modules/@tauri-apps/cli/config.schema.json",
  "productName": "Happy (preview)",
  "identifier": "com.slopus.happy.preview",
  "build": {
    "beforeBuildCommand": "APP_ENV=preview pnpm exec expo export --platform web --output-dir dist"
  },
  "app": {
    "windows": [
      {
        "title": "Happy (preview)"
      }
    ]
  }
}
