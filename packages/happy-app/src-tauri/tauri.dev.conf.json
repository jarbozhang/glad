{
  "$schema": "../../node_modules/@tauri-apps/cli/config.schema.json",
  "productName": "BFELAB (dev)",
  "identifier": "com.bfelab.desktop.dev",
  "build": {
    "beforeDevCommand": "pnpm exec cross-env APP_ENV=development APP_TARGET=desktop pnpm start",
    "beforeBuildCommand": "pnpm exec cross-env APP_ENV=development APP_TARGET=desktop pnpm exec expo export --platform web --output-dir dist"
  },
  "app": {
    "windows": [
      {
        "title": "BFELAB (dev)",
        "width": 1680,
        "height": 1050,
        "titleBarStyle": "Overlay",
        "trafficLightPosition": {
          "x": 22,
          "y": 22
        },
        "hiddenTitle": true
      }
    ]
  }
}
